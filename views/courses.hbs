<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Extracted Courses</title>
    <style>
        .course-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-around;
        }

        .course {
            width: 200px;
            height: 200px;
            border: 1px solid #ccc;
            margin: 10px;
            padding: 10px;
            box-sizing: border-box;
        }

        .course img {
            max-width: 100%;
            max-height: 100px;
            display: block;
            margin: 0 auto 10px;
        }
    </style>
</head>

<body>
    <h1>Extracted Courses</h1>
    <div class="course-container">
        {{#each courses}}
        <div class="course">
            <h2>{{this.name}}</h2>
            <img src="{{this.image}}" alt="{{this.name}}">
            <p>Type: {{this.type}}</p>
            <p>Duration: {{this.duration}} minutes</p>
            <p>Category ID: {{this.categories_id}}</p>
            <p>Category Code: {{this.categories_code}}</p>
            <a href="{{this.url}}" target="_blank">Go to course</a>
        </div>
        {{/each}}
    </div>
</body>

</html>